#ifndef PARSERUTIL_H_
#define PARSERUTIL_H_

#include <boost/tokenize.hpp>

#include <string>
#include <vector>

class ParserUtil {
  static std::vector<std::string> tokenize(std::string inputLine, char delimiter) {
    boost::tokenize<char> 
  }
}

#endif // PARSERUTIL_H_
